# Phase 1 実装指示書 — ローカルファイル I/O 層

- 作成日: 2026-05-13
- 作成者: manager-agent
- 対象エージェント: coding-agent
- 情報源:
  - `inbox/2026-05-12_実装計画_MVP.md`
  - `inbox/2026-05-13_phase0_サマリ.md`
  - `システムプロンプト.md`（リポジトリルート直下）
  - `data/profile.json` / `data/equipment.json` / `data/pantry.json` / `data/meal_history.jsonl`

---

## このフェーズで起動するエージェント

| エージェント | 役割 | 理由 |
|---|---|---|
| Manager-agent | 指示書作成 | 本ドキュメント |
| Coding-agent | 実装 | Phase 1 全タスクの実装担当 |
| Code-Review-agent | レビュー | 実装後、標準サイクル通りレビューを実施 |
| Security-Check-agent | セキュリティ監査 | File System Access API でユーザーのローカルファイルを書き換えるコードを含む。profile.json（嗜好・健康情報）/ meal_history.jsonl（個人行動履歴）を加工するコードを含む。構成.md の発動条件に該当 |
| Content-agent | ユーザー向けサマリ作成 | フェーズ完了時の標準サイクルにより毎フェーズ末で実施 |

---

## 概要

Phase 0 で確立した基盤（Vite + React + TypeScript）の上に、ローカルファイルの読み書きを担う I/O 層を構築する。

このフェーズでは **チャット UI や LLM 連携は一切実装しない**。
ファイルアクセス・状態管理・書き込みユーティリティの基盤を完成させることに集中する。

---

## 作業範囲（今回実施するタスク一覧）

1. Vite テンプレート初期ファイルのクリーンアップ（App.tsx / App.css / index.css の差し替え）
2. `package.json` への `typecheck` スクリプト追加
3. TypeScript 型定義ファイルの作成（`src/types/data.types.ts`）
4. ファイル I/O ユーティリティの作成（`src/lib/fileio.ts`）
5. アプリ状態コンテキストの作成（`src/providers/AppStateContext.tsx`）
6. フォルダ選択フックの作成（`src/hooks/useFolderPicker.ts`）
7. データ読み込みフックの作成（`src/hooks/useDataLoader.ts`）
8. ファイル書き込みフックの作成（`src/hooks/useFileSaver.ts`）
9. フォルダ選択 UI コンポーネントの作成（`src/components/FolderPicker.tsx`）
10. エラー通知コンポーネントの作成（`src/components/ErrorBanner.tsx`）
11. `src/App.tsx` の更新（フォルダ選択・読み込み結果の最低限の確認表示を組み込む）
12. `src/main.tsx` の確認（`AppStateContext` のプロバイダーをラップ）

---

## Phase 0 からの申し送り事項（必ず対処すること）

### 申し送り 1: Vite テンプレート初期ファイルのクリーンアップ

タスク 1 で対処する。以下のファイルをアプリ固有の実装へ差し替える。

- `app/src/App.tsx` — カウンターデモを完全に撤去し、Phase 1 用のルートコンポーネントに差し替える（タスク 11 を参照）
- `app/src/App.css` — 内容を全消去してから必要最小限のスタイルのみ記述する
- `app/src/index.css` — 内容を全消去してから CSS リセット相当の最小限スタイルのみ記述する
- `app/src/assets/react.svg` — 削除対象（**削除は main Claude が Bash で実行する。coding-agent は削除しない**）

### 申し送り 2: `const enum` 使用禁止

`tsconfig.app.json` に `erasableSyntaxOnly: true` が設定されているため、**`const enum` を使うとビルドエラーになる**。

TypeScript の enum が必要な場合は以下のいずれかを使うこと：

```typescript
// 推奨: as const オブジェクト（型消去可能、Vite の ESbuild とも互換性が高い）
const SaveMode = {
  Auto: 'auto',
  Manual: 'manual',
} as const;
type SaveMode = (typeof SaveMode)[keyof typeof SaveMode];

// 代替: 通常の enum（型消去可能なため erasableSyntaxOnly に抵触しない）
enum SaveMode {
  Auto = 'auto',
  Manual = 'manual',
}
```

`const enum` だけが禁止であり、通常の `enum` は使用可能。ただし `as const` オブジェクトを優先することを推奨する。

### 申し送り 3: `typecheck` スクリプトの追加

`package.json` の `scripts` セクションに以下を追加する（タスク 2 で対処）：

```json
"typecheck": "tsc -b --noEmit"
```

---

## 設計判断（manager-agent が決定済み）

以下の判断は manager-agent が独断で決定した（新規依存追加なし・軽微な範囲内）。

| 項目 | 決定内容 | 理由 |
|---|---|---|
| 状態管理 | React Context API（新規ライブラリ不使用） | Phase 1 の状態量は少ない。Zustand / Jotai は Phase 2 以降で検討 |
| ディレクトリハンドル永続化 | セッション中のみメモリ保持（IndexedDB 使用しない） | Phase 1 スコープ内で安全に完結できる範囲に絞る。ハンドルの永続化は Phase 5 以降で検討 |
| 保存モード設定の保管場所 | localStorage（キー: `meshisuke_save_mode`） | 設定値は小さく、セッションをまたいで保持したい。依存追加なしで実現可能 |
| JSONL の読み書き | 全件読み込み / 追記専用書き込み | meal_history.jsonl は現状 7 行程度。ストリーミングは MVP 段階で不要。書き込みは末尾追記のみ対応 |
| スキーマバリデーション | ライブラリ不使用。TypeScript の型ガード関数で対応 | zod 等の追加は人間の判断事項のため独断できない。型ガードで最低限の検証を行う |
| エラーハンドリング表示 | `ErrorBanner` コンポーネントによるインライン表示 | トースト通知ライブラリ（react-hot-toast 等）の追加は人間の判断事項のため独断できない |
| TypeScript 型定義ファイル | `src/types/data.types.ts` に 4 ファイル分の型を集約 | Phase 1 の型量は少なく、分割より集約が見通しよい。Phase 2 以降で分割を検討 |
| サブフェーズ分割 | 分割なし。Phase 1 一本で進める | タスク数は 12 だが相互依存が強く、1a/1b 分割は逆に複雑になる |

---

## タスク詳細

---

### タスク 1: Vite テンプレート初期ファイルのクリーンアップ

#### 1-A: `app/src/App.css` の差し替え

既存の内容をすべて消去し、以下の内容に差し替える。

```css
/* App.css — Phase 1 minimal styles */
.app {
  min-height: 100vh;
  padding: 2rem;
  font-family: sans-serif;
}

.error-banner {
  background: #fee2e2;
  border: 1px solid #fca5a5;
  color: #991b1b;
  padding: 0.75rem 1rem;
  border-radius: 6px;
  margin-bottom: 1rem;
}

.folder-picker {
  display: flex;
  flex-direction: column;
  gap: 1rem;
  padding: 2rem;
  border: 2px dashed #d1d5db;
  border-radius: 8px;
  text-align: center;
}

.folder-picker button {
  padding: 0.75rem 1.5rem;
  background: #2563eb;
  color: white;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  font-size: 1rem;
}

.folder-picker button:hover {
  background: #1d4ed8;
}

.data-summary {
  margin-top: 1.5rem;
  padding: 1rem;
  background: #f0fdf4;
  border: 1px solid #86efac;
  border-radius: 6px;
}
```

#### 1-B: `app/src/index.css` の差し替え

既存の内容をすべて消去し、以下の内容に差し替える。

```css
/* index.css — CSS reset */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  line-height: 1.5;
  -webkit-font-smoothing: antialiased;
}
```

#### 1-C: `app/src/App.tsx` の差し替え（後続タスク完了後に実施）

タスク 9〜10（コンポーネント作成）が完了した後に差し替える。内容はタスク 11 に記載。

---

### タスク 2: `package.json` への `typecheck` スクリプト追加

`app/package.json` の `scripts` セクションに `"typecheck"` を追加する。

```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "format": "prettier --write src/",
    "typecheck": "tsc -b --noEmit"
  }
}
```

`"typecheck"` の 1 行を追加するだけ。他の scripts は変更しない。

---

### タスク 3: TypeScript 型定義ファイルの作成

**作成ファイル: `app/src/types/data.types.ts`**

4 つのデータファイルに対応する TypeScript 型を定義する。
スキーマは `システムプロンプト.md`（リポジトリルート直下）の「内部実装メモ」セクションと実際の `data/*.json` に基づく。

```typescript
// --------------------------------------------------------
// profile.json
// --------------------------------------------------------

export type WeightedItem = {
  name: string;
  weight: 1 | 2 | 3;
  last_signal_at: string; // YYYY-MM-DD
  signal_count: number;
};

export type DailyTargets = {
  calories_kcal: number;
  protein_g: number;
  fat_g: number;
  carbohydrate_g: number;
  salt_g: number;
  note: string;
};

export type Profile = {
  onboarding_completed: boolean;
  created_at: string; // YYYY-MM-DD
  updated_at: string; // YYYY-MM-DD
  allergies: WeightedItem[] | null; // null = 未確認
  dislikes: WeightedItem[] | null;  // null = 未確認
  favorites: WeightedItem[] | null; // null = 未確認
  dietary_restrictions: string[];
  daily_targets: DailyTargets;
  notes: string;
};

// --------------------------------------------------------
// equipment.json
// --------------------------------------------------------

export type Appliance = {
  name: string;
};

export type Cookware = {
  name: string;
  size?: string;
};

export type HeatSource = {
  name: string;
  burners?: number;
};

export type Equipment = {
  updated_at: string;
  appliances: Appliance[];
  cookware: Cookware[];
  heat_sources: HeatSource[];
  utensils: string[];
  skill_level: string;
  constraints: string[];
};

// --------------------------------------------------------
// pantry.json
// --------------------------------------------------------

export type StorageType = '冷蔵' | '冷凍' | '常温' | '野菜室';

export type Ingredient = {
  name: string;
  quantity: number;
  unit: string;
  purchased_at: string;
  expires_at: string;
  storage: StorageType;
  note: string;
};

export type Seasoning = {
  name: string;
  amount_current: number;
  amount_full: number;
  unit: 'ml' | 'g';
  note: string;
};

export type Pantry = {
  updated_at: string;
  ingredients: Ingredient[];
  seasonings: Seasoning[];
};

// --------------------------------------------------------
// meal_history.jsonl（1行 = 1食）
// --------------------------------------------------------

export type MealType = '朝' | '昼' | '夜' | '間食';
export type NutritionSource = '推定' | 'MEXT 八訂';

export type NutritionRecord = {
  kcal: number;
  protein_g: number;
  fat_g: number;
  carb_g: number;
  salt_g: number;
  source: NutritionSource;
};

export type MealIngredientUsed = {
  name: string;
  quantity: number;
  unit: string;
};

export type MealRecord = {
  date: string; // YYYY-MM-DD
  meal: MealType;
  menu: string;
  ingredients_used: MealIngredientUsed[];
  nutrition: NutritionRecord;
  cost_yen: number;
  satisfaction: number | null; // 1〜5 または null（後日入力）
  note: string;
};

// --------------------------------------------------------
// アプリ全体の状態型
// --------------------------------------------------------

export const SaveMode = {
  Auto: 'auto',
  Manual: 'manual',
} as const;
export type SaveMode = (typeof SaveMode)[keyof typeof SaveMode];

export type AppDataState = {
  profile: Profile | null;
  equipment: Equipment | null;
  pantry: Pantry | null;
  mealHistory: MealRecord[];
};

export type AppState = {
  data: AppDataState;
  saveMode: SaveMode;
  isLoading: boolean;
  errors: string[];
  dirHandle: FileSystemDirectoryHandle | null;
};
```

---

### タスク 4: ファイル I/O ユーティリティの作成

**作成ファイル: `app/src/lib/fileio.ts`**

React に依存しない純粋なユーティリティ関数群。
File System Access API の直接操作と、JSON / JSONL の変換を担う。

```typescript
import type { MealRecord } from '../types/data.types';

// --------------------------------------------------------
// 型ガード関数（スキーマ検証用）
// --------------------------------------------------------

/**
 * ファイル名 → ファイルハンドルを取得する
 * data/ 配下のハンドルを想定（dirHandle は data/ ディレクトリ）
 */
export async function getFileHandle(
  dirHandle: FileSystemDirectoryHandle,
  fileName: string
): Promise<FileSystemFileHandle> {
  return dirHandle.getFileHandle(fileName);
}

// --------------------------------------------------------
// JSON ファイルの読み込み
// --------------------------------------------------------

/**
 * FileSystemFileHandle から JSON を読み込んでパースして返す
 * パース失敗時は null を返す（エラーはログのみ）
 */
export async function readJsonFile<T>(
  fileHandle: FileSystemFileHandle
): Promise<T | null> {
  try {
    const file = await fileHandle.getFile();
    const text = await file.text();
    return JSON.parse(text) as T;
  } catch (err) {
    console.error(`[fileio] readJsonFile failed: ${fileHandle.name}`, err);
    return null;
  }
}

// --------------------------------------------------------
// JSONL ファイルの読み込み
// --------------------------------------------------------

/**
 * FileSystemFileHandle から JSONL を読み込んで行ごとにパースして返す
 * パース失敗行はスキップしてコンソールにログを出す
 */
export async function readJsonlFile<T>(
  fileHandle: FileSystemFileHandle
): Promise<T[]> {
  try {
    const file = await fileHandle.getFile();
    const text = await file.text();
    const lines = text
      .split('\n')
      .map((line) => line.trim())
      .filter((line) => line.length > 0);

    const results: T[] = [];
    for (const [i, line] of lines.entries()) {
      try {
        results.push(JSON.parse(line) as T);
      } catch {
        console.warn(`[fileio] readJsonlFile: skipped malformed line ${i + 1} in ${fileHandle.name}`);
      }
    }
    return results;
  } catch (err) {
    console.error(`[fileio] readJsonlFile failed: ${fileHandle.name}`, err);
    return [];
  }
}

// --------------------------------------------------------
// JSON ファイルの書き込み
// --------------------------------------------------------

/**
 * オブジェクトを JSON として上書き保存する
 * 保存失敗時は Error をスローする（呼び出し側でキャッチすること）
 */
export async function writeJsonFile<T>(
  fileHandle: FileSystemFileHandle,
  data: T
): Promise<void> {
  const writable = await fileHandle.createWritable();
  try {
    await writable.write(JSON.stringify(data, null, 2));
    await writable.close();
  } catch (err) {
    await writable.abort();
    throw err;
  }
}

// --------------------------------------------------------
// JSONL ファイルへの追記
// --------------------------------------------------------

/**
 * MealRecord を JSONL ファイルの末尾に 1 行追記する
 * 追記失敗時は Error をスローする（呼び出し側でキャッチすること）
 */
export async function appendJsonlRecord(
  fileHandle: FileSystemFileHandle,
  record: MealRecord
): Promise<void> {
  // 現在の内容を全件読み込んで末尾に 1 行追加してから上書き保存
  // File System Access API には "append mode" がないため、全件読み込み→追記→上書きで実現する
  const file = await fileHandle.getFile();
  const existingText = await file.text();
  const trimmed = existingText.trimEnd();
  const newLine = JSON.stringify(record);
  const newContent = trimmed.length > 0 ? `${trimmed}\n${newLine}\n` : `${newLine}\n`;

  const writable = await fileHandle.createWritable();
  try {
    await writable.write(newContent);
    await writable.close();
  } catch (err) {
    await writable.abort();
    throw err;
  }
}

// --------------------------------------------------------
// data/ ディレクトリの検出
// --------------------------------------------------------

/**
 * ルートフォルダハンドルから data/ サブディレクトリハンドルを取得する
 * data/ が存在しない場合は null を返す
 */
export async function findDataDirectory(
  rootHandle: FileSystemDirectoryHandle
): Promise<FileSystemDirectoryHandle | null> {
  try {
    const dataHandle = await rootHandle.getDirectoryHandle('data');
    return dataHandle;
  } catch {
    return null;
  }
}

// --------------------------------------------------------
// 4ファイルの存在確認
// --------------------------------------------------------

export const REQUIRED_FILES = [
  'profile.json',
  'equipment.json',
  'pantry.json',
  'meal_history.jsonl',
] as const;

export type RequiredFileName = (typeof REQUIRED_FILES)[number];

/**
 * data/ ディレクトリに必須ファイルが存在するか確認する
 * 存在しないファイル名のリストを返す（空配列 = 全て存在）
 */
export async function checkRequiredFiles(
  dataHandle: FileSystemDirectoryHandle
): Promise<RequiredFileName[]> {
  const missing: RequiredFileName[] = [];
  for (const fileName of REQUIRED_FILES) {
    try {
      await dataHandle.getFileHandle(fileName);
    } catch {
      missing.push(fileName);
    }
  }
  return missing;
}

// --------------------------------------------------------
// 型ガード（スキーマ検証の最低限）
// --------------------------------------------------------

/**
 * profile.json が最低限の必須フィールドを持つか確認する型ガード
 * 詳細なフィールド検証は行わない（MVP スコープの最低限）
 */
export function isValidProfile(value: unknown): boolean {
  if (typeof value !== 'object' || value === null) return false;
  const obj = value as Record<string, unknown>;
  return (
    typeof obj['onboarding_completed'] === 'boolean' &&
    typeof obj['created_at'] === 'string' &&
    typeof obj['updated_at'] === 'string'
  );
}

/**
 * equipment.json が最低限の必須フィールドを持つか確認する型ガード
 */
export function isValidEquipment(value: unknown): boolean {
  if (typeof value !== 'object' || value === null) return false;
  const obj = value as Record<string, unknown>;
  return (
    typeof obj['updated_at'] === 'string' &&
    Array.isArray(obj['appliances']) &&
    Array.isArray(obj['cookware']) &&
    Array.isArray(obj['heat_sources'])
  );
}

/**
 * pantry.json が最低限の必須フィールドを持つか確認する型ガード
 */
export function isValidPantry(value: unknown): boolean {
  if (typeof value !== 'object' || value === null) return false;
  const obj = value as Record<string, unknown>;
  return (
    typeof obj['updated_at'] === 'string' &&
    Array.isArray(obj['ingredients']) &&
    Array.isArray(obj['seasonings'])
  );
}
```

---

### タスク 5: アプリ状態コンテキストの作成

**作成ファイル: `app/src/providers/AppStateContext.tsx`**

アプリ全体の状態（読み込んだデータ・保存モード・エラー一覧・ディレクトリハンドル）を React Context で提供する。

```typescript
import { createContext, useContext, useReducer, type ReactNode } from 'react';
import {
  SaveMode as SaveModeConst,
  type AppState,
  type AppDataState,
  type SaveMode,
  type MealRecord,
  type Profile,
  type Pantry,
  type Equipment,
} from '../types/data.types';

// --------------------------------------------------------
// 初期状態
// --------------------------------------------------------

const initialState: AppState = {
  data: {
    profile: null,
    equipment: null,
    pantry: null,
    mealHistory: [],
  },
  saveMode: (localStorage.getItem('meshisuke_save_mode') as SaveMode) ?? SaveModeConst.Auto,
  isLoading: false,
  errors: [],
  dirHandle: null,
};

// --------------------------------------------------------
// Action 型定義
// --------------------------------------------------------

type Action =
  | { type: 'SET_DIR_HANDLE'; payload: FileSystemDirectoryHandle }
  | { type: 'SET_DATA'; payload: AppDataState }
  | { type: 'SET_LOADING'; payload: boolean }
  | { type: 'ADD_ERROR'; payload: string }
  | { type: 'CLEAR_ERRORS' }
  | { type: 'SET_SAVE_MODE'; payload: SaveMode }
  | { type: 'UPDATE_PROFILE'; payload: Profile }
  | { type: 'UPDATE_PANTRY'; payload: Pantry }
  | { type: 'UPDATE_EQUIPMENT'; payload: Equipment }
  | { type: 'ADD_MEAL_RECORD'; payload: MealRecord };

// --------------------------------------------------------
// Reducer
// --------------------------------------------------------

function appReducer(state: AppState, action: Action): AppState {
  switch (action.type) {
    case 'SET_DIR_HANDLE':
      return { ...state, dirHandle: action.payload };

    case 'SET_DATA':
      return { ...state, data: action.payload };

    case 'SET_LOADING':
      return { ...state, isLoading: action.payload };

    case 'ADD_ERROR':
      return { ...state, errors: [...state.errors, action.payload] };

    case 'CLEAR_ERRORS':
      return { ...state, errors: [] };

    case 'SET_SAVE_MODE': {
      localStorage.setItem('meshisuke_save_mode', action.payload);
      return { ...state, saveMode: action.payload };
    }

    case 'UPDATE_PROFILE':
      return {
        ...state,
        data: { ...state.data, profile: action.payload },
      };

    case 'UPDATE_PANTRY':
      return {
        ...state,
        data: { ...state.data, pantry: action.payload },
      };

    case 'UPDATE_EQUIPMENT':
      return {
        ...state,
        data: { ...state.data, equipment: action.payload },
      };

    case 'ADD_MEAL_RECORD':
      return {
        ...state,
        data: {
          ...state.data,
          mealHistory: [...state.data.mealHistory, action.payload],
        },
      };

    default:
      return state;
  }
}

// --------------------------------------------------------
// Context
// --------------------------------------------------------

type AppStateContextValue = {
  state: AppState;
  dispatch: React.Dispatch<Action>;
};

const AppStateContext = createContext<AppStateContextValue | null>(null);

export function AppStateProvider({ children }: { children: ReactNode }) {
  const [state, dispatch] = useReducer(appReducer, initialState);
  return (
    <AppStateContext.Provider value={{ state, dispatch }}>
      {children}
    </AppStateContext.Provider>
  );
}

export function useAppState(): AppStateContextValue {
  const ctx = useContext(AppStateContext);
  if (ctx === null) {
    throw new Error('useAppState must be used within AppStateProvider');
  }
  return ctx;
}
```

---

### タスク 6: フォルダ選択フックの作成

**作成ファイル: `app/src/hooks/useFolderPicker.ts`**

ユーザーがフォルダを選択するロジックを切り出したカスタムフック。

```typescript
import { useCallback } from 'react';
import { useAppState } from '../providers/AppStateContext';
import { findDataDirectory, checkRequiredFiles } from '../lib/fileio';

export function useFolderPicker() {
  const { dispatch } = useAppState();

  const pickFolder = useCallback(async () => {
    // File System Access API 対応チェック
    if (!('showDirectoryPicker' in window)) {
      dispatch({
        type: 'ADD_ERROR',
        payload:
          'このブラウザは File System Access API に対応していません。Chrome または Edge をお使いください。',
      });
      return;
    }

    try {
      // ルートフォルダ選択ダイアログを開く
      const rootHandle = await window.showDirectoryPicker({ mode: 'readwrite' });
      dispatch({ type: 'SET_DIR_HANDLE', payload: rootHandle });

      // data/ サブディレクトリを探す
      const dataHandle = await findDataDirectory(rootHandle);
      if (dataHandle === null) {
        dispatch({
          type: 'ADD_ERROR',
          payload:
            '選択されたフォルダに data/ ディレクトリが見つかりませんでした。めし助リポジトリのルートフォルダを選択してください。',
        });
        return;
      }

      // 必須ファイルの存在確認
      const missing = await checkRequiredFiles(dataHandle);
      if (missing.length > 0) {
        dispatch({
          type: 'ADD_ERROR',
          payload: `data/ フォルダに必要なファイルが見つかりません: ${missing.join(', ')}`,
        });
        return;
      }

      // 必須ファイル確認がすべて通ったら rootHandle を state に保持する
      // useDataLoader は state.dirHandle を見て loadData() を呼ぶ（FolderPicker の useEffect 経由）
      // dataHandle は loadData() 内で findDataDirectory を再度呼んで取得する
      dispatch({ type: 'SET_DIR_HANDLE', payload: rootHandle });

    } catch (err) {
      // ユーザーがキャンセルした場合（AbortError）はエラーとして扱わない
      if (err instanceof DOMException && err.name === 'AbortError') {
        return;
      }
      dispatch({
        type: 'ADD_ERROR',
        payload: `フォルダの読み込み中にエラーが発生しました: ${String(err)}`,
      });
    }
  }, [dispatch]);

  return { pickFolder };
}
```

---

### タスク 7: データ読み込みフックの作成

**作成ファイル: `app/src/hooks/useDataLoader.ts`**

`dirHandle`（ルートフォルダハンドル）が設定された後に 4 ファイルを読み込むフック。

```typescript
import { useCallback } from 'react';
import { useAppState } from '../providers/AppStateContext';
import {
  findDataDirectory,
  getFileHandle,
  readJsonFile,
  readJsonlFile,
  isValidProfile,
  isValidEquipment,
  isValidPantry,
} from '../lib/fileio';
import type { Profile, Equipment, Pantry, MealRecord } from '../types/data.types';

export function useDataLoader() {
  const { state, dispatch } = useAppState();

  const loadData = useCallback(async () => {
    const rootHandle = state.dirHandle;
    if (rootHandle === null) return;

    dispatch({ type: 'SET_LOADING', payload: true });
    dispatch({ type: 'CLEAR_ERRORS' });

    try {
      // data/ ディレクトリハンドル取得
      const dataHandle = await findDataDirectory(rootHandle);
      if (dataHandle === null) {
        dispatch({ type: 'ADD_ERROR', payload: 'data/ ディレクトリが見つかりません。' });
        return;
      }

      const errors: string[] = [];

      // profile.json 読み込み
      let profile: Profile | null = null;
      try {
        const fh = await getFileHandle(dataHandle, 'profile.json');
        const raw = await readJsonFile<unknown>(fh);
        if (raw !== null && isValidProfile(raw)) {
          profile = raw as Profile;
        } else {
          errors.push('profile.json のスキーマが不正です。');
        }
      } catch {
        errors.push('profile.json の読み込みに失敗しました。');
      }

      // equipment.json 読み込み
      let equipment: Equipment | null = null;
      try {
        const fh = await getFileHandle(dataHandle, 'equipment.json');
        const raw = await readJsonFile<unknown>(fh);
        if (raw !== null && isValidEquipment(raw)) {
          equipment = raw as Equipment;
        } else {
          errors.push('equipment.json のスキーマが不正です。');
        }
      } catch {
        errors.push('equipment.json の読み込みに失敗しました。');
      }

      // pantry.json 読み込み
      let pantry: Pantry | null = null;
      try {
        const fh = await getFileHandle(dataHandle, 'pantry.json');
        const raw = await readJsonFile<unknown>(fh);
        if (raw !== null && isValidPantry(raw)) {
          pantry = raw as Pantry;
        } else {
          errors.push('pantry.json のスキーマが不正です。');
        }
      } catch {
        errors.push('pantry.json の読み込みに失敗しました。');
      }

      // meal_history.jsonl 読み込み
      let mealHistory: MealRecord[] = [];
      try {
        const fh = await getFileHandle(dataHandle, 'meal_history.jsonl');
        mealHistory = await readJsonlFile<MealRecord>(fh);
      } catch {
        errors.push('meal_history.jsonl の読み込みに失敗しました。');
      }

      // エラーを dispatch
      for (const msg of errors) {
        dispatch({ type: 'ADD_ERROR', payload: msg });
      }

      // データを dispatch（一部が null でもエラーが出た分は ADD_ERROR 済みなので続行）
      dispatch({
        type: 'SET_DATA',
        payload: { profile, equipment, pantry, mealHistory },
      });
    } finally {
      dispatch({ type: 'SET_LOADING', payload: false });
    }
  }, [state.dirHandle, dispatch]);

  return { loadData };
}
```

---

### タスク 8: ファイル書き込みフックの作成

**作成ファイル: `app/src/hooks/useFileSaver.ts`**

JSON ファイルの上書き保存と JSONL への追記を行うフック。

```typescript
import { useCallback } from 'react';
import { useAppState } from '../providers/AppStateContext';
import {
  findDataDirectory,
  getFileHandle,
  writeJsonFile,
  appendJsonlRecord,
} from '../lib/fileio';
import type { Profile, Pantry, Equipment, MealRecord } from '../types/data.types';
import { SaveMode } from '../types/data.types';

export function useFileSaver() {
  const { state, dispatch } = useAppState();

  /**
   * profile.json を保存する
   * saveMode === 'auto' の場合は即座に書き込む
   * saveMode === 'manual' の場合はコンテキストの状態のみ更新し、ファイルへの書き込みは行わない
   */
  const saveProfile = useCallback(
    async (profile: Profile) => {
      dispatch({ type: 'UPDATE_PROFILE', payload: profile });

      if (state.saveMode !== SaveMode.Auto) return;
      if (state.dirHandle === null) return;

      try {
        const dataHandle = await findDataDirectory(state.dirHandle);
        if (dataHandle === null) throw new Error('data/ ディレクトリが見つかりません。');
        const fh = await getFileHandle(dataHandle, 'profile.json');
        await writeJsonFile(fh, profile);
      } catch (err) {
        dispatch({ type: 'ADD_ERROR', payload: `profile.json の保存に失敗しました: ${String(err)}` });
      }
    },
    [state.dirHandle, state.saveMode, dispatch]
  );

  /**
   * pantry.json を保存する（saveMode に従う）
   */
  const savePantry = useCallback(
    async (pantry: Pantry) => {
      dispatch({ type: 'UPDATE_PANTRY', payload: pantry });

      if (state.saveMode !== SaveMode.Auto) return;
      if (state.dirHandle === null) return;

      try {
        const dataHandle = await findDataDirectory(state.dirHandle);
        if (dataHandle === null) throw new Error('data/ ディレクトリが見つかりません。');
        const fh = await getFileHandle(dataHandle, 'pantry.json');
        await writeJsonFile(fh, pantry);
      } catch (err) {
        dispatch({ type: 'ADD_ERROR', payload: `pantry.json の保存に失敗しました: ${String(err)}` });
      }
    },
    [state.dirHandle, state.saveMode, dispatch]
  );

  /**
   * equipment.json を保存する（saveMode に従う）
   */
  const saveEquipment = useCallback(
    async (equipment: Equipment) => {
      dispatch({ type: 'UPDATE_EQUIPMENT', payload: equipment });

      if (state.saveMode !== SaveMode.Auto) return;
      if (state.dirHandle === null) return;

      try {
        const dataHandle = await findDataDirectory(state.dirHandle);
        if (dataHandle === null) throw new Error('data/ ディレクトリが見つかりません。');
        const fh = await getFileHandle(dataHandle, 'equipment.json');
        await writeJsonFile(fh, equipment);
      } catch (err) {
        dispatch({ type: 'ADD_ERROR', payload: `equipment.json の保存に失敗しました: ${String(err)}` });
      }
    },
    [state.dirHandle, state.saveMode, dispatch]
  );

  /**
   * meal_history.jsonl に 1 行追記する（saveMode に従う）
   */
  const appendMealRecord = useCallback(
    async (record: MealRecord) => {
      dispatch({ type: 'ADD_MEAL_RECORD', payload: record });

      if (state.saveMode !== SaveMode.Auto) return;
      if (state.dirHandle === null) return;

      try {
        const dataHandle = await findDataDirectory(state.dirHandle);
        if (dataHandle === null) throw new Error('data/ ディレクトリが見つかりません。');
        const fh = await getFileHandle(dataHandle, 'meal_history.jsonl');
        await appendJsonlRecord(fh, record);
      } catch (err) {
        dispatch({ type: 'ADD_ERROR', payload: `meal_history.jsonl の追記に失敗しました: ${String(err)}` });
      }
    },
    [state.dirHandle, state.saveMode, dispatch]
  );

  /**
   * 手動保存モード時: バッファに溜まったすべての変更をファイルに書き出す
   * 自動保存モードでは各 save* が即座に書き込むため、この関数は手動モード専用
   */
  const flushAll = useCallback(async () => {
    if (state.dirHandle === null) return;

    const { profile, equipment, pantry } = state.data;
    const errors: string[] = [];

    try {
      const dataHandle = await findDataDirectory(state.dirHandle);
      if (dataHandle === null) throw new Error('data/ ディレクトリが見つかりません。');

      if (profile !== null) {
        try {
          const fh = await getFileHandle(dataHandle, 'profile.json');
          await writeJsonFile(fh, profile);
        } catch (err) {
          errors.push(`profile.json: ${String(err)}`);
        }
      }

      if (equipment !== null) {
        try {
          const fh = await getFileHandle(dataHandle, 'equipment.json');
          await writeJsonFile(fh, equipment);
        } catch (err) {
          errors.push(`equipment.json: ${String(err)}`);
        }
      }

      if (pantry !== null) {
        try {
          const fh = await getFileHandle(dataHandle, 'pantry.json');
          await writeJsonFile(fh, pantry);
        } catch (err) {
          errors.push(`pantry.json: ${String(err)}`);
        }
      }

      // meal_history は appendMealRecord が IN-MEMORY に追加したレコードをファイルに書く
      // 全件上書きではなく、「前回読み込み時以降に追加されたレコード分」を追記する必要があるが、
      // Phase 1 では meal_history の手動フラッシュは対象外とする。
      // （Phase 3 で LLM の応答から実際にレコードが追加されるタイミングで再検討する）

    } catch (err) {
      errors.push(String(err));
    }

    for (const msg of errors) {
      dispatch({ type: 'ADD_ERROR', payload: `保存エラー: ${msg}` });
    }
  }, [state.dirHandle, state.data, dispatch]);

  return { saveProfile, savePantry, saveEquipment, appendMealRecord, flushAll };
}
```

---

### タスク 9: フォルダ選択 UI コンポーネントの作成

**作成ファイル: `app/src/components/FolderPicker.tsx`**

フォルダ未選択時に表示するコンポーネント。

```typescript
import { useEffect } from 'react';
import { useFolderPicker } from '../hooks/useFolderPicker';
import { useDataLoader } from '../hooks/useDataLoader';
import { useAppState } from '../providers/AppStateContext';

export function FolderPicker() {
  const { state } = useAppState();
  const { pickFolder } = useFolderPicker();
  const { loadData } = useDataLoader();

  // ディレクトリハンドルが設定されたら自動的にデータを読み込む
  useEffect(() => {
    if (state.dirHandle !== null) {
      void loadData();
    }
  }, [state.dirHandle, loadData]);

  if (state.dirHandle !== null && !state.isLoading && state.errors.length === 0) {
    // 読み込み完了後はこのコンポーネントは表示しない（App.tsx が切り替える）
    return null;
  }

  return (
    <div className="folder-picker">
      <h2>めし助へようこそ</h2>
      <p>めし助リポジトリのルートフォルダを選択してください。</p>
      <p>
        <small>（data/ フォルダが含まれているフォルダを選択してください）</small>
      </p>
      <button type="button" onClick={() => void pickFolder()} disabled={state.isLoading}>
        {state.isLoading ? '読み込み中...' : 'フォルダを選択する'}
      </button>
    </div>
  );
}
```

---

### タスク 10: エラー通知コンポーネントの作成

**作成ファイル: `app/src/components/ErrorBanner.tsx`**

エラーがある場合にバナー表示するコンポーネント。

```typescript
import { useAppState } from '../providers/AppStateContext';

export function ErrorBanner() {
  const { state, dispatch } = useAppState();

  if (state.errors.length === 0) return null;

  return (
    <div className="error-banner" role="alert">
      <strong>エラーが発生しました:</strong>
      <ul>
        {state.errors.map((msg, i) => (
          <li key={i}>{msg}</li>
        ))}
      </ul>
      <button
        type="button"
        onClick={() => dispatch({ type: 'CLEAR_ERRORS' })}
        style={{ marginTop: '0.5rem', cursor: 'pointer' }}
      >
        閉じる
      </button>
    </div>
  );
}
```

---

### タスク 11: `app/src/App.tsx` の差し替え

Vite テンプレートのカウンターデモを完全に撤去し、Phase 1 の動作確認用ルートコンポーネントに差し替える。

```typescript
import { useAppState } from './providers/AppStateContext';
import { FolderPicker } from './components/FolderPicker';
import { ErrorBanner } from './components/ErrorBanner';
import './App.css';

function App() {
  const { state } = useAppState();
  const { data, dirHandle, isLoading } = state;

  const isDataLoaded =
    dirHandle !== null &&
    !isLoading &&
    data.profile !== null;

  return (
    <div className="app">
      <h1>めし助</h1>
      <ErrorBanner />

      {!isDataLoaded ? (
        <FolderPicker />
      ) : (
        <div className="data-summary">
          <h2>データ読み込み完了</h2>
          <p>オンボーディング: {data.profile?.onboarding_completed ? '完了' : '未完了'}</p>
          <p>食材数: {data.pantry?.ingredients.length ?? 0} 件</p>
          <p>調味料数: {data.pantry?.seasonings.length ?? 0} 件</p>
          <p>食事履歴: {data.mealHistory.length} 件</p>
          <p>調理器具数: {data.equipment?.appliances.length ?? 0} 件（家電）</p>
        </div>
      )}
    </div>
  );
}

export default App;
```

---

### タスク 12: `app/src/main.tsx` の更新

`AppStateProvider` でアプリ全体をラップする。
まず現在の `main.tsx` を読み込んでから編集すること。

変更内容:

- `import { AppStateProvider } from './providers/AppStateContext'` を追加
- `<App />` を `<AppStateProvider><App /></AppStateProvider>` に変更

**変更後の `main.tsx` の形（参考）**:

```typescript
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.tsx'
import { AppStateProvider } from './providers/AppStateContext.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <AppStateProvider>
      <App />
    </AppStateProvider>
  </StrictMode>,
)
```

`main.tsx` は Vite テンプレートが生成した内容から変わっている可能性があるため、実際のファイルを確認してから最小限の編集を行うこと。

---

## 進行上の分担

coding-agent は `Bash` を持たないため、以下の分担とする。

### main Claude が Bash で実行する作業

```powershell
# app/src/assets/react.svg の削除
Remove-Item "C:\Github\meshisuke\app\src\assets\react.svg"

# 実装後の動作確認コマンド
cd C:\Github\meshisuke\app
npm run typecheck
npm run lint
npm run build
npm run dev
```

### coding-agent が Write / Edit で実行する作業

- タスク 1〜12 のすべてのファイル作成・編集

---

## 実装時の注意事項

1. **`app/` 外のファイルには手を触れない**
   - `data/*.json` / `data/*.jsonl` / `システムプロンプト.md` / `README.md` はスコープ外
   - coding-agent がファイルを読む（Read）のは許容するが、書く（Write / Edit）のは `app/` 配下のみ

2. **`const enum` は使用禁止**（申し送り 2 参照）

3. **`noUnusedLocals: true` / `noUnusedParameters: true` が有効**
   - インポートしたが使わない型・関数はビルドエラーになる
   - Phase 1 では使われない関数（例: `flushAll` は UI から呼ぶボタンなしでテストのみ）も型エラーにならないように export して型整合性を保つこと

4. **File System Access API の型定義**
   - `window.showDirectoryPicker` の型は `@types/node` には含まれていない
   - TypeScript の `lib: ["DOM"]` に含まれているため、`tsconfig.app.json` の現在の設定で型補完が効く
   - 型定義が解決されない場合は `tsconfig.app.json` の `types` 配列を確認すること

5. **`verbatimModuleSyntax: true` が有効**
   - 型のみのインポートは `import type { ... }` で記述すること（`import { ... }` では型と値が混在しているとビルドエラーになる場合がある）

6. **エラーハンドリングの方針**
   - ユーザーがキャンセルしたとき（`AbortError`）はエラーとして表示しない
   - ファイルが見つからない・パース失敗は `ADD_ERROR` で通知する
   - 通知 UI は `ErrorBanner` コンポーネントで行う

---

## 完了条件（受け入れテスト）

以下をすべて満たした時点で Phase 1 完了とする。

1. `npm run typecheck` がエラーなく完了する（warning は許容）
2. `npm run lint` がエラーなく完了する（warning は許容）
3. `npm run build` が成功する（`dist/` に本番バンドルが生成される）
4. `npm run dev` でブラウザを開くと「めし助」のタイトルとフォルダ選択 UI が表示される（Vite カウンターデモは表示されない）
5. フォルダ選択ボタンをクリックすると OS のフォルダ選択ダイアログが開く
6. `C:\Github\meshisuke`（めし助リポジトリルート）を選択すると、エラーなくデータが読み込まれる
7. 読み込み完了後、画面に「データ読み込み完了」と食材数・調味料数・食事履歴数が表示される（正しい件数が表示されること: 食材 13 件、調味料 14 件、食事履歴 7 件）
8. 存在しないフォルダ（`data/` を持たないフォルダ）を選択した場合、エラーバナーにエラーメッセージが表示される
9. `app/src/App.tsx` に Vite テンプレートのカウンターデモ関連コード（`count`, `setCount`, `reactLogo`, `viteLogo` の参照）が残っていない
10. `app/package.json` の `scripts` に `"typecheck": "tsc -b --noEmit"` が存在する

---

## 報告事項

Phase 1 完了後、coding-agent は以下を `inbox/` に報告ファイルとして作成すること。

- ファイル名: `2026-05-13_phase1_完了報告.md`
- 含めるべき内容:
  - 作成・変更したファイルの一覧
  - 完了条件の確認結果（各項目: 確認済み / 未確認 / NG）
  - 実装中に指示書から逸脱した点・判断が必要だった点とその対応
  - 次フェーズ (Phase 2) への申し送り事項

---

## 人間への確認事項

以下の事項は manager-agent の独断範囲を超えるため、**main Claude がユーザーに確認**してから Phase 2 以降に反映してほしい。

### 確認 1: 状態管理ライブラリの選定（Phase 2 着手前に決定）

Phase 1 では React Context API + useReducer で実装する。
Phase 2 以降でチャット履歴・LLM 接続状態・ストリーミング状態などが加わると状態量が増える。

- **A案**: このまま React Context API を拡張し続ける（依存追加なし、設計次第でスケールする）
- **B案**: Zustand を追加する（新規依存追加。`npm install zustand`）
- **C案**: Jotai を追加する（新規依存追加。`npm install jotai`）

Phase 1 完了後のレビュー報告を見てから決めても問題ないが、Phase 2 の指示書作成前に方針を決めておきたい。

### 確認 2: エラー通知 UI の方針（Phase 3 着手前に決定）

Phase 1 では `ErrorBanner`（インラインバナー）で対応する。
Phase 3 でチャット UI が完成するとトースト通知（画面隅にポップアップ）の方が UX として自然になる可能性がある。

- **A案**: インラインバナーを使い続ける（追加依存なし）
- **B案**: `react-hot-toast` 等のトースト通知ライブラリを追加する（新規依存追加）

Phase 3 着手前に決定してほしい。

### 確認 3: IndexedDB によるディレクトリハンドル永続化の要否（Phase 5 以降の検討）

File System Access API のハンドルは IndexedDB に保存することで、ページを再読み込みしても再選択なしでアクセスできる。
Phase 1 では「毎セッション起動時にフォルダ選択」とする。

- **A案**: 毎セッション選択で割り切る（実装シンプル）
- **B案**: Phase 5 でハンドルを IndexedDB に永続化する（利便性が上がる）

MVP の段階で永続化が必要かどうかを判断してほしい。

---

*指示書終わり*
