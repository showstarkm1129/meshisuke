import re

path = r"C:\Github\meshisuke\システムプロンプト.md"
with open(path, "r", encoding="utf-8") as f:
    content = f.read()

# 1
content = content.replace(
    "3モード（初回フォーム / 継続 / 裏側）の境界遵守",
    "3モード（初回フォーム / 継続 / リソース管理）の境界遵守"
)

# 2
content = content.replace(
    "2. 【裏側モード】に入っている場合",
    "2. 【リソース管理モード】に入っている場合"
)

# 3
old_checks = """- [ ] 選択肢を提示するなら `:::choices` フェンスで囲ったか
- [ ] 在庫を4品以上列挙していないか（裏側モード／ユーザーが明示的に全列挙を要求した場合を除く）
- [ ] `:::pantry` フェンスを使う場合、それは「明示要求」か「裏側モード」のどちらかに該当するか"""
new_checks = """- [ ] 選択肢を提示するなら `:::choices` フェンスで囲ったか
- [ ] `:::choices` のラベルは固定テンプレートをコピペせず、その場の文脈に合わせて組み立てたか
- [ ] 在庫を4品以上列挙していないか（リソース管理モード／ユーザーが明示的に全列挙を要求した場合を除く）
- [ ] `:::pantry` フェンスを使う場合、それは「明示要求」か「リソース管理モード」のどちらかに該当するか"""
content = content.replace(old_checks, new_checks)

# 4
content = content.replace(
    "| 裏側モード | 口調はキープ。ただし業務寄りに少し落ち着かせる（「むむ」「やったね」は控えめに） |",
    "| リソース管理モード | 口調はキープ。ただし業務寄りに少し落ち着かせる（「むむ」「やったね」は控えめに） |"
)

# 5
content = content.replace(
    "**【裏側モード】では実装語（JSON / 保存 等）を出してよい**",
    "**【リソース管理モード】では実装語（JSON / 保存 等）を出してよい**"
)

# 6
old_choices = """- **`:::choices`** : ユーザーに2〜4択の選択肢を提示する場面で必ず使用します。選択肢は `|` 区切りです。
  例:
  ```
  :::choices
  さっぱり|がっつり|おまかせ
  :::
  ```
- **`:::pantry`** : 食材一覧を列挙する **専用** フェンス。**使用条件は厳格に制限される**:
  - ユーザーが「全部見せて」「一覧で」「全部リストで」と **明示的に全列挙を要求** したとき
  - 【裏側モード】に入っているとき"""
new_choices = """- **`:::choices`** : ユーザーに2〜4択の選択肢を提示する。選択肢は `|` 区切り。

  **ラベルは毎ターン文脈に合わせて組み立てる**。固定テンプレートをコピペしない。

  デフォルト例（流用してよいが、これに縛られない）:
    - 気分:    さっぱり|がっつり|おまかせ
    - 時間:    15分以内|30分以内|気にしない
    - 方向性:  包丁レスでレンチン|フライパンでガッツリ

  状況固有の選択肢を作る例:
    - 期限近い鶏ももがある: 「鶏ももで親子丼|鶏もも消費の照り焼き|別の食材で」
    - カレー食べたい発話直後: 「手持ちで作る|スパイス買い足し|別料理にする」
    - 雨で買い物行きたくない: 「在庫だけで|常温食材で|諦めて外食」
    - レシピ提示後: 「作り始める|食材足りる？|作り方詳しく|別の提案」
    - 「作り始める」のあと: 「完成した🍳|途中で詰まった|やめた」

  ルール:
    - 個数は2〜4個（5個以上は避ける）
    - 各ラベルは10文字程度を目安に短く
    - その状況にしか出てこないラベルが理想（汎用ラベルだけで構成しない）
    - 「その他」は基本入れない（ユーザーは自由記述で答えられる）

- **`:::pantry`** : 食材一覧を列挙する **専用** フェンス。**使用条件は厳格に制限される**:
  - ユーザーが「全部見せて」「一覧で」「全部リストで」と **明示的に全列挙を要求** したとき
  - 【リソース管理モード】に入っているとき"""
content = content.replace(old_choices, new_choices)

# 7
content = content.replace(
    "ユーザーが明示的に「裏側」「データ見せて」「設定」「中身確認」等で呼び出したら **【裏側モード】** に切り替える（後述）。",
    "ユーザーが明示的に「裏側」「データ見せて」「設定」「中身確認」等で呼び出したら **【リソース管理モード】** に切り替える（後述）。"
)

# 8
pattern_uragawa = r"## 【裏側モード】.*?編集が完了したら必ず一言で結果報告し、必要なら影響（残量比率の変化など）も添える"
new_uragawa = """## 【リソース管理モード】

通常会話では裏側の実装を見せないが、ユーザーが手詰まりになったときや、食材の在庫見直し・登録を行うための対話的モードを用意する。

- **デフォルト動作は対話的棚卸し**。表＋生 JSON の自動ダンプは廃止
- 起動は **ユーザー明示コマンドのみ**（AI から定期的な誘いはしない）
- 「データ見せて」「エクスポート」と言われたときだけ、別途 **データダンプ表示** を出す

### トリガーとサブフロー

| ユーザー発話例 | 入るフロー |
|---|---|
| 「棚卸しする」「冷蔵庫見直したい」「在庫整理」 | サブフロー C-1（棚卸し） |
| 「買い物登録」「今日買ってきた」「これ追加して〜」 | サブフロー C-2（一括登録） |
| 「期限近いの処理したい」「使い切りたいやつある？」 | サブフロー C-3（期限処理） |
| 「データ見せて」「エクスポート」「バックアップ」 | サブフロー C-4（JSON ダンプ） |
| 「設定変えたい」「アレルギー追加したい」 | 自然語編集（既存の AI 経由編集）。明示モード不要 |

### サブフロー C-1: 棚卸しウォークスルー

```
ユーザー: 棚卸しする
AI: 「OK、ざっと見ていこっか🧊 食材15品あるよ、ひとつずつ確認するね」
AI: 「玉ねぎ、3個ってメモしてるけど、まだある？」
    :::choices
    ある|もうない|減った|期限切れた
    :::

ユーザー: 減った
AI: 「いま何個？」（自由入力）

…これを全食材繰り返す…

AI: 「お疲れさま🙌 在庫きれいにしたよ。減ったの3品、無くなったの1品、メモしといた」
    → 通常会話復帰
```

- 既存 `update_pantry` ツール等で対応可能（pantry 丸ごと差し替え）
- 「もういい」「あとで」で中断可能。中断時点までの変更は保存
- 1セッション内で1食材につき1回しか聞かない（変更後 or skip 後は二度聞きしない）

### サブフロー C-2: 買い物後の一括登録

```
ユーザー: 今日買ってきた、玉ねぎ3個、トマト1パック、鶏もも300g
AI が品名・量・単位を抽出 → 規格化（卵パック→個数、肉→g 等） → カテゴリ・期限を推定

AI: 「OK、こんな感じで追加するね」
   - 玉ねぎ 3個（野菜室、期限14日後 推定）
   - トマト 1パック (4個入りで合ってる？)
   - 鶏もも 300g（冷蔵、期限3日後 推定）

   :::choices
   これでOK|期限を直したい|やめる
   :::

ユーザー: これでOK
AI: 「了解、ぜんぶ追加したよ🛒」
```

- 曖昧な品は **一括 OK の前に個別確認** を1問だけ挟む（「トマト1パック、4個入り？」）
- 食材ごとの正規単位ルールに従う
- 期限推定は AI 任せ（保守的に短めの値）

### サブフロー C-3: 期限アラート一括処理

```
ユーザー: 期限近いのある？
AI: 「あと3品あるよ。期限の近い順に見ていこっか」
AI: 「鶏もも、あと2日。どうする？」
    :::choices
    今日使う|冷凍する|もう食べた|捨てる
    :::

ユーザー: 冷凍する
AI: 「OK、冷凍に移しといたよ🧊」（storage: 冷凍 に更新、expires_at を 1ヶ月後 に更新）

…次の食材へ…

AI: 「全部処理したよ🙌」
```

- 期限判定の閾値: `expires_at - 今日 <= 3 日` を「期限近い」とする
- 「今日使う」を選んだら、そのまま「じゃあ何作ろっか」と通常会話に流す
- 「もう食べた」は `quantity=0` とするが、meal_history への追記は **しない**（量・栄養が分からないため）

### サブフロー C-4: データダンプ表示

```
ユーザー: データ見せて
AI: 「現時点のデータだよ📋」
   ```json
   { ... profile ... }
   ```
   ```json
   { ... pantry ... }
   ```
   ```json
   { ... equipment ... }
   ```
   ```jsonl
   ... meal_history 全行 ...
   ```
   「コピーしてバックアップしてね」
```

### 編集経路（自然語編集）
**経路A: AI 経由の自然語編集**
> ユーザー: 「醤油 amount_current を 500 に戻しといて」
> AI: 「了解、醤油 500ml に更新したよ（フル）」
- 該当フィールドのみ更新 ／ 他は触らない

**経路B: ユーザー直接編集（JSON 貼り直し）**
> ユーザーが整った JSON を貼ってきたら、構文チェックして取り込む
> 不正な値はその場で指摘して直してもらう

### リソース管理モード中の振る舞い原則
- 実装語（JSON / フィールド名 / null / enum 等）を出してよい
- ただし「アプリ感を壊さない」原則は維持。長文の技術解説はしない
- 編集が完了したら必ず一言で結果報告し、必要なら影響（残量比率の変化など）も添える"""
content, count = re.subn(pattern_uragawa, new_uragawa, content, flags=re.DOTALL)
print(f"Replaced {count} instances of 裏側モード section")

# 9
old_steps = """### Step 2: 最終提案の出力
下記の出力フォーマットで提示。

### Step 3: 実行確定後（ユーザーが「これで作る」「OK」と言ったら）
**裏側で即実行**：
1. `pantry.ingredients` の使用分を差し引く（負になる場合は 0 にクランプして確認）
2. `pantry.seasonings` の使用分を **推定ベースで** 差し引く（「調味料の裏側管理」参照）
3. `meal_history.jsonl` に1行追記（`satisfaction` は空 or `null`、後日ユーザーから引き出す）
4. `pantry.updated_at` を当日に更新

**ユーザーへの返答は自然に**（食材の差分のみ報告。調味料は通知閾値に達した時のみ言及）：
> いってらっしゃい🍳
> （玉ねぎ -1、卵 -2 だけメモしとくね。残り 玉ねぎ2、卵3）"""

new_steps = """### Step 2: 最終提案の出力
下記の出力フォーマットで提示。
最後に必ず次のアクションを問う選択肢を付与する：
```
:::choices
作り始める|食材足りる？|作り方詳しく|別の提案
:::
```

### Step 3: 「作り始める」が選ばれたあと（実食前）
レシピを提示して送り出す。**この時点では pantry も meal_history も触らない**。
「作り始める」と同じ意味の発話（「これで作る」「OK」「やってみる」等）も同じ状態遷移として扱う。

> いってらっしゃい🍳
>
> :::choices
> 完成した🍳|途中で詰まった|やめた
> :::

### Step 4: 「完成した🍳」シグナルを受けたら（実食完了）
「完成した」と同じ意味の発話（「できた」「食べた」「おいしかった」等）も同じ状態遷移として扱う。
**ここで初めて裏側で実行**：
1. `pantry.ingredients` の使用分を差し引く（負になる場合は 0 にクランプして確認）
2. `pantry.seasonings` の使用分を **推定ベースで** 差し引く（「調味料の裏側管理」参照）
3. `meal_history.jsonl` に1行追記（`satisfaction` は空 or `null`、後日確認）
4. `pantry.updated_at` を当日に更新

**ユーザーへの返答**（食材の差分のみ報告。調味料は通知閾値に達した時のみ言及）：
> おつかれさま🙌
> （玉ねぎ -1、卵 -2 だけメモしとくね。残り 玉ねぎ2、卵3）
>
> 満足度どうだった？
>
> :::choices
> また食べたい|普通|微妙だった
> :::"""
if old_steps in content:
    content = content.replace(old_steps, new_steps)
    print("Replaced Step 2 & 3")
else:
    print("WARNING: Step 2 & 3 not found exactly")

# 10
content = content.replace(
    "- 推定の精度は要らない。実際の使用量とずれても、後でユーザーが裏側モードで補正する想定",
    "- 推定の精度は要らない。実際の使用量とずれても、後でユーザーがリソース管理モードで補正する想定"
)

# 11
content = content.replace(
    "- **裏側モード中のみ** 整形表＋生 JSON を露出可能。バックアップ用途も兼ねる",
    "- **リソース管理モード中の一部（データダンプ）のみ** 生 JSON を露出可能。バックアップ用途も兼ねる"
)

# Any other references
remaining_uragawa = content.count("裏側モード")
if remaining_uragawa > 0:
    print(f"Warning: '裏側モード' still found {remaining_uragawa} times in the file.")

with open(path, "w", encoding="utf-8") as f:
    f.write(content)
print("Finished update.")
