# プロジェクト構造・サブエージェント運用

本ドキュメントは、めし助プロジェクトにおけるディレクトリ構成と、
Claude Code のサブエージェント運用ルールを定義する。
個別の開発原則・禁止事項は [CLAUDE.md](./CLAUDE.md) を参照。

---

## ディレクトリ構成

- `app/` — フロントエンド本体（React + Vite + TypeScript）
- `data/` — ユーザーデータの実例（仮想テストユーザー）
  - `profile.json` / `equipment.json` / `pantry.json` / `meal_history.jsonl`
- `inbox/` — 作業報告・指示書・レビュー報告などを集約する作業ログ置き場
  - **任意運用**。フェーズ完了時に「残しておく価値がある成果物」だけ保存する。
  - 義務的に毎フェーズ書類を生成する場ではない。
- `.claude/agents/` — サブエージェント定義（後述）
- `.claude/agent-memory/` — 各エージェントの永続メモリ

---

## 役割分担

### 私（人間）
- 作りたいもの・要件を提示する。
- アプリケーションの機能・実装に関する **最終的な意思決定** を行う。

### main Claude（中心役）
- 本プロジェクトの **主たる実装者**。日常の編集・実装・動作確認は main Claude が直接行う。
- 担当: ユーザー対話、軽〜中規模の実装、軽微修正の即時処理、動作確認、サブエージェント呼び出し判断。
- 大規模変更や独立した責務が必要な場合のみサブエージェントを呼ぶ（次節）。

### サブエージェント（条件付き呼び出し）

| エージェント | 稼働状況 | 呼び出し条件 |
|---|---|---|
| `security-check-agent` | **稼働** | 後述のトリガー条件に該当する変更が入ったとき |
| `code-review-agent` | **稼働** | PR 相当のまとまった変更を区切りでレビューしたいとき／main Claude が独立視点を欲したとき |
| `coding-agent` | **稼働（低頻度）** | main Claude のコンテキストを圧迫するほど大きな実装を分離したいとき |
| `manager-agent` | **休止中** | 3 つ以上の独立サブタスクを並列実行する必要が出てきた時に再稼働を検討 |
| `content-agent` | **必要時のみ** | README・アーキテクチャ図など、対外ドキュメントを刷新する節目のみ |

> エージェント定義ファイル（`.claude/agents/*.md`）は **削除せず保持** する。
> プロジェクト規模が拡大した際に運用を再開できるようにしておく。

---

## `security-check-agent` を呼ぶトリガー条件

以下のいずれかに該当する変更が入ったとき、main Claude が判断して呼び出す。

- BYO API キーの取扱コード（保存・読み出し・送信先指定）
- `localStorage` / `IndexedDB` / `Cookie` への書き込み
- 外部 HTTP リクエストを発行するコード
- File System Access API でユーザーのローカルファイルを書き換えるコード
- `profile.json` など個人情報（嗜好・健康情報含む）を加工するコード

---

## サブエージェント呼び出し時の共通プロトコル

サブエージェントを呼び出す際、main Claude は以下を必ず指示書に含める。

1. **目的・成功基準** — 何が達成できれば完了か
2. **対象スコープ** — 触ってよいファイル／触ってはいけないファイル
3. **必読ファイル** — 最低限 [CLAUDE.md](./CLAUDE.md) と本ファイル
4. **期待される成果物** — レビュー報告書 / 実装差分 / セキュリティ監査レポート 等
5. **報告先** — 必要なら `inbox/YYYY-MM-DD_<topic>.md` に成果物を残す

サブエージェント側の追加必読：

| エージェント | 追加で読むべきもの |
|---|---|
| `coding-agent` | タスク指示書 + 編集対象ファイルの現状 |
| `code-review-agent` | 指示書 + 実装差分 + 対象ファイル全文 + 関連する上位ファイル（設定変更時は `.gitignore` / CLAUDE.md 等） |
| `security-check-agent` | 対象ファイル + API キー・外部通信・PII を扱う関連コード一式 |
| `content-agent` | 説明対象の成果物全体 + 既存ドキュメント |

---

## 標準ワークフロー

固定パイプラインは設けない。main Claude がタスク内容に応じて柔軟に組む。

典型例：

- **小〜中規模の実装** → main Claude が直接実装 → 自己検証（[CLAUDE.md](./CLAUDE.md) コア原則 2）→ 完了
- **個人情報・APIキー・外部通信に触れる実装** → main Claude が実装 → `security-check-agent` で監査 → 指摘あれば main Claude が修正
- **PR レベルの区切り** → main Claude が `code-review-agent` で独立レビューを依頼 → 指摘を main Claude が反映
- **対外ドキュメント刷新の節目** → `content-agent` で README / アーキ図を更新

「全フェーズで Manager → Coding → Review → Security → Content を順番に通す」運用は廃止。
そのフェーズに必要なステップだけを実行する。
